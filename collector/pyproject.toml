[project]
name = "lawncare-collector"
version = "0.1.0"
description = "Weather data collector for the lawncare dashboard"
requires-python = ">=3.11"
dependencies = [
    "requests==2.33.1",
    "schedule==1.2.2",
]
